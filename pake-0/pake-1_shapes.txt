(comment "CPSA 4.4.6")
(comment "Extracted shapes")

(herald "PAKE 1" (algebra diffie-hellman))

(comment "CPSA 4.4.6")

(comment "All input read from pake-1.scm")

(defprotocol pake1 diffie-hellman
  (defrole init
    (vars (a b name) (pw data) (alpha beta rndx))
    (trace (send (exp (gen) alpha)) (recv (exp (gen) beta))
      (send
        (enc "hello" b "I am" a
          (hash pw a b (exp (gen) alpha) (exp (gen) beta)
            (exp (gen) (mul alpha beta)))))
      (recv
        (enc "got it" a "I am" b
          (hash pw a b (exp (gen) alpha) (exp (gen) beta)
            (exp (gen) (mul alpha beta)))))))
  (defrole resp
    (vars (a b name) (pw data) (alpha beta rndx))
    (trace (recv (exp (gen) alpha)) (send (exp (gen) beta))
      (recv
        (enc "hello" b "I am" a
          (hash pw a b (exp (gen) alpha) (exp (gen) beta)
            (exp (gen) (mul alpha beta)))))
      (send
        (enc "got it" a "I am" b
          (hash pw a b (exp (gen) alpha) (exp (gen) beta)
            (exp (gen) (mul alpha beta)))))))
  (defgenrule neqRl_indx
    (forall ((x indx)) (implies (fact neq x x) (false))))
  (defgenrule neqRl_strd
    (forall ((x strd)) (implies (fact neq x x) (false))))
  (defgenrule neqRl_mesg
    (forall ((x mesg)) (implies (fact neq x x) (false)))))

(defskeleton pake1
  (vars (pw data) (a b name) (alpha beta rndx))
  (defstrand init 4 (pw pw) (a a) (b b) (alpha alpha) (beta beta))
  (pen-non-orig pw)
  (uniq-gen alpha)
  (traces
    ((send (exp (gen) alpha)) (recv (exp (gen) beta))
      (send
        (enc "hello" b "I am" a
          (hash pw a b (exp (gen) alpha) (exp (gen) beta)
            (exp (gen) (mul alpha beta)))))
      (recv
        (enc "got it" a "I am" b
          (hash pw a b (exp (gen) alpha) (exp (gen) beta)
            (exp (gen) (mul alpha beta)))))))
  (label 0)
  (unrealized (0 3))
  (origs)
  (ugens (alpha (0 0)))
  (comment "2 in cohort - 2 not yet seen"))

(defskeleton pake1
  (vars (pw data) (a b name) (alpha beta rndx))
  (defstrand init 4 (pw pw) (a a) (b b) (alpha alpha) (beta beta))
  (defstrand resp 4 (pw pw) (a a) (b b) (alpha alpha) (beta beta))
  (precedes ((0 0) (1 0)) ((0 2) (1 2)) ((1 3) (0 3)))
  (pen-non-orig pw)
  (uniq-gen alpha)
  (operation encryption-test (displaced 2 0 init 3)
    (enc "hello" b "I am" a
      (hash pw a b (exp (gen) alpha) (exp (gen) beta)
        (exp (gen) (mul alpha beta)))) (1 2))
  (strand-map 0 1)
  (traces
    ((send (exp (gen) alpha)) (recv (exp (gen) beta))
      (send
        (enc "hello" b "I am" a
          (hash pw a b (exp (gen) alpha) (exp (gen) beta)
            (exp (gen) (mul alpha beta)))))
      (recv
        (enc "got it" a "I am" b
          (hash pw a b (exp (gen) alpha) (exp (gen) beta)
            (exp (gen) (mul alpha beta))))))
    ((recv (exp (gen) alpha)) (send (exp (gen) beta))
      (recv
        (enc "hello" b "I am" a
          (hash pw a b (exp (gen) alpha) (exp (gen) beta)
            (exp (gen) (mul alpha beta)))))
      (send
        (enc "got it" a "I am" b
          (hash pw a b (exp (gen) alpha) (exp (gen) beta)
            (exp (gen) (mul alpha beta)))))))
  (label 3)
  (parent 0)
  (realized)
  (shape)
  (maps ((0) ((a a) (b b) (pw pw) (alpha alpha) (beta beta))))
  (origs)
  (ugens (alpha (0 0))))

(comment "Nothing left to do")

(defprotocol pake1 diffie-hellman
  (defrole init
    (vars (a b name) (pw data) (alpha beta rndx))
    (trace (send (exp (gen) alpha)) (recv (exp (gen) beta))
      (send
        (enc "hello" b "I am" a
          (hash pw a b (exp (gen) alpha) (exp (gen) beta)
            (exp (gen) (mul alpha beta)))))
      (recv
        (enc "got it" a "I am" b
          (hash pw a b (exp (gen) alpha) (exp (gen) beta)
            (exp (gen) (mul alpha beta)))))))
  (defrole resp
    (vars (a b name) (pw data) (alpha beta rndx))
    (trace (recv (exp (gen) alpha)) (send (exp (gen) beta))
      (recv
        (enc "hello" b "I am" a
          (hash pw a b (exp (gen) alpha) (exp (gen) beta)
            (exp (gen) (mul alpha beta)))))
      (send
        (enc "got it" a "I am" b
          (hash pw a b (exp (gen) alpha) (exp (gen) beta)
            (exp (gen) (mul alpha beta)))))))
  (defgenrule neqRl_indx
    (forall ((x indx)) (implies (fact neq x x) (false))))
  (defgenrule neqRl_strd
    (forall ((x strd)) (implies (fact neq x x) (false))))
  (defgenrule neqRl_mesg
    (forall ((x mesg)) (implies (fact neq x x) (false)))))

(defskeleton pake1
  (vars (pw data) (a b name) (beta alpha rndx))
  (defstrand resp 4 (pw pw) (a a) (b b) (alpha alpha) (beta beta))
  (pen-non-orig pw)
  (uniq-gen beta)
  (traces
    ((recv (exp (gen) alpha)) (send (exp (gen) beta))
      (recv
        (enc "hello" b "I am" a
          (hash pw a b (exp (gen) alpha) (exp (gen) beta)
            (exp (gen) (mul beta alpha)))))
      (send
        (enc "got it" a "I am" b
          (hash pw a b (exp (gen) alpha) (exp (gen) beta)
            (exp (gen) (mul beta alpha)))))))
  (label 7)
  (unrealized (0 2))
  (origs)
  (ugens (beta (0 1)))
  (comment "2 in cohort - 2 not yet seen"))

(defskeleton pake1
  (vars (pw data) (a b name) (alpha beta rndx))
  (defstrand resp 4 (pw pw) (a a) (b b) (alpha alpha) (beta beta))
  (defstrand init 3 (pw pw) (a a) (b b) (alpha alpha) (beta beta))
  (precedes ((0 1) (1 1)) ((1 2) (0 2)))
  (pen-non-orig pw)
  (uniq-gen beta)
  (operation encryption-test (added-strand init 3)
    (enc "hello" b "I am" a
      (hash pw a b (exp (gen) alpha) (exp (gen) beta)
        (exp (gen) (mul alpha beta)))) (0 2))
  (strand-map 0)
  (traces
    ((recv (exp (gen) alpha)) (send (exp (gen) beta))
      (recv
        (enc "hello" b "I am" a
          (hash pw a b (exp (gen) alpha) (exp (gen) beta)
            (exp (gen) (mul alpha beta)))))
      (send
        (enc "got it" a "I am" b
          (hash pw a b (exp (gen) alpha) (exp (gen) beta)
            (exp (gen) (mul alpha beta))))))
    ((send (exp (gen) alpha)) (recv (exp (gen) beta))
      (send
        (enc "hello" b "I am" a
          (hash pw a b (exp (gen) alpha) (exp (gen) beta)
            (exp (gen) (mul alpha beta)))))))
  (label 8)
  (parent 7)
  (realized)
  (shape)
  (maps ((0) ((a a) (b b) (pw pw) (beta beta) (alpha alpha))))
  (origs)
  (ugens (beta (0 1))))

(comment "Nothing left to do")
